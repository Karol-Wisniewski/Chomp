{
	"name": "server",
	"version": "1.0.0",
	"description": "",
	"main": "server.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "nodemon ./src/server.js",
		"prettier": "prettier --write ."
	},
	"type": "module",
	"author": "",
	"license": "ISC",
	"dependencies": {
		"nodemon": "^3.0.2",
		"ws": "^8.16.0"
	},
	"devDependencies": {
		"prettier": "^3.1.1"
	}
}
